# Screenshot release assets

This directory intentionally contains no fabricated screenshots. Before publishing the repository, capture the locally running application and add these files:

- `landing-page.png`
- `upload-page.png`
- `investigation-timeline.png`
- `dashboard.png`
- `executive-report.png`
- `razorpay-sync.png`

The README references these as release-asset slots. Do not replace them with mockups; capture the actual app after launching the bundled demo investigation.
