import { CheckCircle2, CircleDot, Timer } from "lucide-react";
import type { TimelineEvent } from "@/lib/types";
import { titleCase } from "@/components/format";

export function Timeline({ events, pending = [] }: { events: TimelineEvent[]; pending?: string[] }) {
  return <ol className="space-y-1">{events.map((event, index) => <li key={event.id} className="relative flex gap-4 pb-5 last:pb-0"><span className="relative z-10 grid h-7 w-7 shrink-0 place-items-center rounded-full bg-emerald-50 text-emerald-700"><CheckCircle2 size={18} /></span>{index < events.length - 1 && <span className="absolute left-[13px] top-7 h-[calc(100%-16px)] w-px bg-emerald-100" />}<div className="min-w-0 pt-0.5"><div className="flex flex-wrap items-center gap-2"><p className="font-semibold text-slate-800">{event.title}</p><span className="rounded-full bg-slate-100 px-2 py-0.5 text-xs font-semibold text-slate-500">{Math.round(event.progress * 100)}%</span></div>{event.description && <p className="mt-1 text-sm leading-6 text-slate-600">{event.description}</p>}</div></li>)}{pending.map((stage, index) => <li key={stage} className="flex gap-4 pb-5 text-slate-500 last:pb-0"><span className="grid h-7 w-7 shrink-0 place-items-center rounded-full border border-slate-300 bg-white">{index === 0 ? <Timer size={14} /> : <CircleDot size={13} />}</span><div className="pt-0.5"><p className="font-semibold">{titleCase(stage)}</p><p className="mt-1 text-sm leading-6">Waiting for the authoritative graph update.</p></div></li>)}</ol>;
}
